"use client";

/**
 * Scroll runway for dark → light reveal.
 * Copy lives on the loader cover; this section drives camera + lighting.
 */
export function CoverSection() {
  return (
    <section
      id="section-cover"
      className="story-section--cover relative"
      aria-label="Enter Manchester"
    />
  );
}
